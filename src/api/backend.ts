import { Product } from './interface'

const API_URL = ""

const getRequest = (url: string) => (path: string) => {
  // TODO
}

const request = (url: string) => (path: string, body: object) => {
  // TODO
}

// const getProducts: Product[]  = () => getRequest(API_URL)("/products")

// export { getProducts }
